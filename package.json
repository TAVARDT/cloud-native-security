{
  "name": "tavardt-hardened-base",
  "version": "1.0.0",
  "description": "Minimal package for CI build validation",
  "scripts": {
    "build": "mkdir -p dist && echo 'console.log(\"Hardened Node Base Ready\")' > dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "commander": "^14.0.3"
  }
}
